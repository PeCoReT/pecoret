services:
  db:
    image: postgres:alpine
    environment:
      POSTGRES_PASSWORD: dev
      POSTGRES_DB: dev
      POSTGRES_USER: dev
    ports:
      - '127.0.0.1:5432:5432'
  redis:
    image: redis:alpine
    ports:
      - '127.0.0.1:6379:6379'
